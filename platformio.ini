[env:uno]
platform = atmelavr
board = uno
framework = arduino
monitor_speed = 9600

; Upload settings
upload_speed = 115200
upload_port = COM3

; Build flags (optional)
build_flags = 
    -Wall
    -Wextra
